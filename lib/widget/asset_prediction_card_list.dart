import 'package:flutter/material.dart';

class AssetPredictionCardListViewWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Prediction List"),
    );
  }
}
