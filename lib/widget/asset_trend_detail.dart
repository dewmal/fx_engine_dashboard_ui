import 'package:flutter/material.dart';

class AssetTrendWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Ast Trend"),
    );
  }
}
